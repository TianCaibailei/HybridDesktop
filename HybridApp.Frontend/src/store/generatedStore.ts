import { create } from 'zustand';

// This is a placeholder store that will be overwritten by the generator.
// It needs to exist for compilation purposes.
export const useAppStore = create(() => ({}));
