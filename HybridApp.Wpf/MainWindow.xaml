<Window x:Class="HybridApp.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HybridApp.Wpf"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>

        <wv2:WebView2 Name="webView" Grid.Column="0" />

        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Vision Settings" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" />

            <TextBlock Text="Exposure" />
            <Slider Minimum="0" Maximum="100" Value="{Binding Exposure, Mode=TwoWay}" />
            <TextBlock Text="{Binding Exposure}" HorizontalAlignment="Right" />

            <TextBlock Text="Gain" Margin="0,10,0,0" />
            <Slider Minimum="0" Maximum="10" Value="{Binding Gain, Mode=TwoWay}" />
            <TextBlock Text="{Binding Gain, StringFormat=F1}" HorizontalAlignment="Right" />

            <CheckBox Content="Is Running" IsChecked="{Binding IsRunning, Mode=TwoWay}" Margin="0,10,0,0" />

            <Separator Margin="0,20" />

            <TextBlock Text="Current State:" FontWeight="Bold" />
            <TextBlock Text="{Binding Exposure, StringFormat='Exposure: {0}'}" />
            <TextBlock Text="{Binding Gain, StringFormat='Gain: {0:F1}'}" />
            <TextBlock Text="{Binding IsRunning, StringFormat='Running: {0}'}" />
        </StackPanel>
    </Grid>
</Window>
